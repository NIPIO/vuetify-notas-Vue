import Vue from 'vue'
import Vuex from 'vuex'
import Vuetify from 'vuetify'

// alert(`Vue version : ${Vuetify.version}`);
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
